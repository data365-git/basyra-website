{
  "name": "basyra-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "extract:standalone": "node tools/extract-bundle.mjs --src \"Basyra Website (standalone) (1).html\" --out out/standalone",
    "extract:bundled": "node tools/extract-bundle.mjs --src \"Bundled Page.html\" --out out/bundled"
  }
}
